// Stdlib only, deliberately: the tool's whole job is git, HTTP, sha256, tar and exec,
// and every one of those is in the standard library. The model does the clever part.
module github.com/gurunars/ff

go 1.25
