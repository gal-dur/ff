# Pushing a vX.Y.Z tag publishes that release, carrying one binary per pinned
# platform (ff-<goos>-<goarch>, which install.sh resolves by uname). A push to
# master alone releases nothing: cutting a version is a deliberate act, and the tag
# is both the trigger and the name.
name: release

on:
  push:
    tags: ["v*.*.*"]

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          # The whole history: the master-ancestry check below walks it.
          fetch-depth: 0

      # A tag is branch-agnostic in git, so without this a version tagged on any
      # stray branch would release. Only what master carries gets published.
      - name: the tag must point into master
        run: |
          git fetch origin master
          git merge-base --is-ancestor "$GITHUB_SHA" origin/master || {
            echo "::error::${{ github.ref_name }} is not on master; refusing to release"
            exit 1
          }

      - uses: actions/setup-go@v5
        with:
          go-version: stable

      - run: go test ./...

      # One binary per pinned platform. The roster and each target's verified
      # llama.cpp archive both come from internal/pin through cmd/pin — this loop
      # holds no copy of the platform list or the fetch-then-verify dance.
      - name: build every platform
        run: |
          for target in $(go run ./cmd/pin platforms); do
            goos=${target%/*} goarch=${target#*/}
            go run ./cmd/pin fetch "$goos" "$goarch"
            CGO_ENABLED=0 GOOS=$goos GOARCH=$goarch \
              go build -trimpath -tags embedruntime \
                -ldflags "-s -w -X main.version=${{ github.ref_name }}" \
                -o "ff-$goos-$goarch" ./cmd/ff
          done

      # The binaries get the same treatment as every artifact ff itself pins:
      # a published checksum, verified by install.sh before anything runs.
      - name: checksums
        run: sha256sum ff-* > SHA256SUMS

      - name: publish
        env:
          GH_TOKEN: ${{ github.token }}
        # A re-run of the same tag finds its release already there; replacing the
        # assets beats failing the whole workflow over idempotence.
        run: |
          gh release create "${{ github.ref_name }}" ff-* SHA256SUMS \
            --title "${{ github.ref_name }}" \
            --notes "ff ${{ github.ref_name }} — one binary per pinned platform" \
          || gh release upload "${{ github.ref_name }}" ff-* SHA256SUMS --clobber
